/*import express from "express";
import {
  initiateEsewaPayment,
  esewaPaymentSuccess,
  esewaPaymentFailure,
} from "../controllers/esewacontroller";

const router = express.Router();

router.post("/:bikeId/bookings/initiate", initiateEsewaPayment);
router.get("/:bikeId/bookings", esewaPaymentSuccess);
router.get("/failure", esewaPaymentFailure);

export default router;
*/
